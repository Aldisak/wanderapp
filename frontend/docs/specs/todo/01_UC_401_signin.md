{
  "$schema": ".claude/schemas/spec.v1.json",
  "uc_id": "UC-401",
  "slug": "signin-and-token-lifecycle",
  "title": "Azure AD B2C sign-in, secure token storage, and Dio auth/refresh interceptor pipeline",
  "actors": [
    "Cold-installed mobile app user signing in for the first time via Azure AD B2C.",
    "Returning user re-launching the app with a previously-stored access + refresh token pair.",
    "User whose access token expires mid-session — the refresh interceptor must transparently refresh and retry without surfacing the 401 to the screen."
  ],
  "preconditions": [
    "Backend UC-201 ('User registration and token refresh via Azure AD B2C') is shipped and reachable: POST /api/v1/auth/register and POST /api/v1/auth/refresh respond per their spec.",
    "Azure AD B2C tenant + app registration are provisioned. B2C client id, tenant name, and policy id are available as --dart-define values: AZURE_AD_B2C_CLIENT_ID, AZURE_AD_B2C_TENANT_NAME, AZURE_AD_B2C_POLICY_ID.",
    "API base URL is available as --dart-define API_BASE_URL.",
    "The Flutter project at frontend/ has Riverpod codegen wired (flutter_riverpod 3.2.1 + riverpod_annotation 4.0.2 + riverpod_generator 4.0.3 + riverpod_lint 3.1.3) and analysis_options.yaml has the riverpod_lint plugin entry. flutter analyze + flutter test are clean before this UC starts.",
    "The frontend has no /sign-in screen yet, no Dio client, no interceptors, no token storage, no router. This UC is the first vertical slice and adds the foundational core/ + features/auth/ infrastructure."
  ],
  "main_flow": [
    "App launches via main.dart → ProviderScope → WanderMeetApp (MaterialApp.router with the GoRouter from lib/app/router.dart). bootstrap.dart initialises Hive (no boxes opened yet — UC-402 owns onboarding_progress), reads --dart-define values into env providers, registers the singleton logger.",
    "GoRouter redirect callback queries authSessionProvider on every navigation: it reads access token from tokenStorageProvider (a flutter_secure_storage facade). If access token is null → redirect to AppRoutes.signIn unless already there.",
    "SignInPage renders: brand mark, single primary button 'Sign in with Wander'. Tapping the button triggers signInControllerProvider.signIn() — an @riverpod AsyncNotifier method.",
    "SignInController.signIn() calls aadOAuthClientProvider.login() (a thin wrapper over package:aad_oauth) which opens the B2C authorization web flow and resolves with { accessToken, refreshToken, idTokenClaims }.",
    "On success, SignInController extracts firstName from idTokenClaims['given_name'] (fallback: idTokenClaims['name']; if both missing → temporary value 'Wanderer' — onboarding will prompt).",
    "SignInController calls authRepositoryProvider.completeSignIn(accessToken, refreshToken, firstName).",
    "AuthApiRepository persists tokens via tokenStorage.write(accessToken, refreshToken) FIRST, then calls authApi.register(RegisterRequest(firstName: firstName)) over Dio (the AuthInterceptor will inject the freshly-stored access token).",
    "Backend returns 201 + UserDto on first registration, OR 409 Auth.AlreadyRegistered on subsequent runs. The repository treats 409 as a non-error path (returning user) — the user is signed in either way. Any other non-2xx becomes an AppFailure subclass.",
    "On success the AuthSession transitions to AuthState.signedIn(userId, firstName, isOnboarded=false). The router's redirect callback re-evaluates and routes to AppRoutes.onboardingWelcome (UC-402's entry point — outside the scope of this UC, but the redirect must point there).",
    "Every subsequent Dio request flows through three interceptors in this exact order: (1) AuthInterceptor injects 'Authorization: Bearer <accessToken>' read fresh from tokenStorage on each request, except for paths matching /api/v1/auth/register or /api/v1/auth/refresh. (2) RefreshInterceptor watches for 401 responses; on 401 it acquires a process-wide Completer<void> (single in-flight refresh), POSTs /api/v1/auth/refresh with the stored refresh token, persists the returned tokens via tokenStorage.write, mutates the failed RequestOptions.headers['Authorization'] to the NEW bearer, and retries via dio.fetch(requestOptions). Concurrent 401s await the same Completer. (3) ApiErrorInterceptor maps non-2xx to typed AppFailure subclasses and rethrows AppFailureException.",
    "Sign-out: settings sign-out tile (or programmatic call from the auth-failure path) calls authSessionProvider.signOut(): tokenStorage.clear(), Hive.deleteBoxFromDisk for non-app_state boxes, authSessionProvider state → signedOut. The router redirect routes to AppRoutes.signIn."
  ],
  "alternate_flows": [
    "User cancels the B2C web flow → aadOAuthClient.login() throws AadCancelledException; SignInController catches and resets state to AsyncData(null) — no error toast, the user simply remains on the sign-in screen.",
    "B2C login succeeds but POST /auth/register returns 401 (server rejects the just-issued B2C token — clock skew, audience mismatch) → repository throws AuthFailure; controller emits AsyncError(AuthFailure); UI shows error message keyed to ApiErrorCodes.authInvalidToken via error_messages.dart.",
    "Refresh fails (refresh token rejected by B2C, or backend /auth/refresh returns 401) → RefreshInterceptor clears tokens, calls authSession.signOut(), and rejects the in-flight Completer with an AppFailureException(AuthFailure). All in-flight requests sharing that Completer also fail. The router's redirect routes to /sign-in. UI shows a one-time toast 'Session expired — please sign in again' (a flag set on authSession.lastSignOutReason).",
    "Two concurrent requests both receive 401 (e.g. discovery + invites tabs both fetching on app foreground) → only one POST /auth/refresh is issued; both requests retry with the same new token.",
    "Cold-launch with stored tokens but POST /users/me returns 401 even after refresh → treated as soft-deleted account. Tokens cleared, sign-out toast 'Account no longer active', redirect /sign-in.",
    "Network unreachable during sign-in → AppFailure subclass NetworkFailure surfaced; UI shows 'No connection' state with a retry button that re-invokes signInControllerProvider.signIn().",
    "User has stored tokens but the registered user record is gone (backend lost it / dev DB reset) → POST /users/me returns 404 + User.NotRegistered → treated as 'needs to register again'. Tokens kept, redirect to /onboarding/welcome (which will hit POST /auth/register on its first interaction). NOTE: this resilience may be deferred to UC-402; UC-401 ships the simpler behaviour: 404 → sign out → /sign-in."
  ],
  "acceptance_criteria": [
    "frontend/lib/main.dart wraps the app in ProviderScope and runs WanderMeetApp from frontend/lib/app/wandermeet_app.dart, which builds MaterialApp.router with the GoRouter from frontend/lib/app/router.dart. The default Flutter counter app is removed.",
    "frontend/lib/app/routes.dart declares AppRoutes (kebab-case path constants: signIn = '/sign-in', onboardingWelcome = '/onboarding/welcome', home = '/') and AppRouteNames (lowerCamelCase route names matching the same set). All navigation in this UC uses context.goNamed(AppRouteNames.x); no string-literal paths at call sites.",
    "frontend/lib/app/router.dart wires a single GoRouter with the auth slice's signIn route + a placeholder home route + a redirect callback that uses authSessionProvider. The redirect rules implemented in this UC: (a) no access token → AppRoutes.signIn (unless current path == signIn); (b) authenticated → AppRoutes.onboardingWelcome (placeholder route returning a 'Onboarding (UC-402)' Scaffold for now); (c) onboarding-incomplete logic and (d) Hive-progress restoration are explicitly out of scope and stubbed.",
    "frontend/lib/core/network/dio_client.dart exposes @riverpod Dio dioClient(...) with baseUrl read from --dart-define API_BASE_URL (no fallback; throws AssertionError at startup if missing). The interceptor list is exactly [AuthInterceptor, RefreshInterceptor, ApiErrorInterceptor] in that order.",
    "AuthInterceptor (frontend/lib/core/network/auth_interceptor.dart) reads the access token from tokenStorageProvider on every onRequest. It skips paths matching '/api/v1/auth/register' and '/api/v1/auth/refresh' (no Bearer header added). On all other paths a missing token short-circuits with a DioException whose error is AuthFailure.",
    "RefreshInterceptor (frontend/lib/core/network/refresh_interceptor.dart) maintains a private Completer<void>? _refreshing field. On 401 with non-refresh path: if _refreshing is null, set it, POST /api/v1/auth/refresh with the stored refresh token, persist returned tokens via tokenStorage.write, complete the Completer, retry the failed RequestOptions (with new Bearer in headers). If _refreshing is non-null on a concurrent 401: await it, then retry. On refresh failure: tokenStorage.clear(), authSession.signOut(reason: lastSignOutReason.sessionExpired), completeError, propagate AppFailureException(AuthFailure).",
    "ApiErrorInterceptor (frontend/lib/core/network/api_error_interceptor.dart) maps Dio status codes to AppFailure subclasses: 0/timeout/no-internet → NetworkFailure, 401 (after refresh exhausted) → AuthFailure, 403 → ForbiddenFailure, 404 → NotFoundFailure, 409 → ConflictFailure, 400 → ValidationFailure (with details map parsed from FastEndpoints' {propertyName: [messages]} body), 429 → RateLimitFailure (with Retry-After parsed), 5xx → ServerFailure. Body parsing extracts the first key whose value is a list of strings — that's the error code (per FastEndpoints AddError convention).",
    "frontend/lib/core/errors/app_failure.dart declares the sealed AppFailure hierarchy exactly as specified in .claude/rules/flutter-architecture.md § 'Error model'. Subclasses: NetworkFailure, AuthFailure, ForbiddenFailure, NotFoundFailure, ConflictFailure, ValidationFailure, RateLimitFailure, ServerFailure. AppFailureException wraps AppFailure for throwing.",
    "frontend/lib/core/errors/api_error_codes.dart adds (at minimum, for this UC) constants mirroring src/WanderMeet.Shared/ErrorCodes.cs: authAlreadyRegistered = 'Auth.AlreadyRegistered', authInvalidToken = 'Auth.InvalidToken' (if defined backend-side; otherwise add backend constant in the same UC), userNotRegistered = 'User.NotRegistered', validationFirstNameRequired = 'Validation.FirstNameRequired', validationFirstNameTooLong = 'Validation.FirstNameTooLong'. Constant string values match the backend exactly.",
    "frontend/lib/core/errors/error_messages.dart provides String messageFor(AppFailure) → user-facing English string keyed off failure.code, with a default fallback. No localization scaffolding yet.",
    "frontend/lib/core/auth/token_storage.dart exposes @riverpod TokenStorage tokenStorage(...) wrapping FlutterSecureStorage with methods Future<String?> readAccessToken(), Future<String?> readRefreshToken(), Future<void> write({required String access, required String refresh}), Future<void> clear(). Tokens are NEVER passed to logger or print(). Storage keys: 'wm.access_token', 'wm.refresh_token'.",
    "frontend/lib/core/auth/aad_oauth_client.dart exposes @riverpod AadOAuthClient aadOAuthClient(...) wrapping package:aad_oauth's AadOAuth. Configured from --dart-define values. Exposes Future<AadLoginResult> login(), Future<void> logout(), Future<String?> currentRefreshToken(). AadLoginResult is a freezed record { accessToken, refreshToken, idTokenClaims (Map<String, Object?>) }.",
    "frontend/lib/core/auth/auth_session.dart exposes @riverpod class AuthSession extends _$AuthSession with @override AuthState build() returning AuthState.signedOut() initially, plus methods Future<void> markSignedIn({required UserDto user}), Future<void> signOut({SignOutReason reason = SignOutReason.userInitiated}). AuthState is sealed: AuthSignedOut, AuthSignedIn(userId, firstName, isOnboarded). The provider has keepAlive: true.",
    "frontend/lib/features/auth/data/auth_api.dart exposes class AuthApi with Future<UserDto> register(RegisterRequest req) → POST /api/v1/auth/register and Future<RefreshTokenResponse> refresh(RefreshTokenRequest req) → POST /api/v1/auth/refresh. Both use the injected Dio.",
    "frontend/lib/features/auth/data/models/{register_request.dart, register_response.dart, refresh_token_request.dart, refresh_token_response.dart, user_dto.dart} are @freezed with fromJson/toJson. UserDto field names mirror backend UserDto exactly: id, firstName, isIdVerified, isOpenToday, isOpenToRomance, trustScore, meetupCount, citiesCount, createdAt.",
    "frontend/lib/features/auth/data/auth_repository.dart declares abstract interface class AuthRepository with Future<UserDto> completeSignIn(String accessToken, String refreshToken, String firstName) and Future<void> signOut(). frontend/lib/features/auth/data/auth_api_repository.dart is the sole implementation: persists tokens BEFORE calling register, treats 409 Auth.AlreadyRegistered as a returning-user happy path (re-fetch /users/me to populate the UserDto — for this UC return a synthetic UserDto with id from JWT sub since /users/me wiring lands in UC-405; document this temporary shortcut with a // TODO(UC-405) comment).",
    "frontend/lib/features/auth/domain/auth_failures.dart declares sealed slice failures extending AppFailure: AuthAlreadyRegistered (extends ConflictFailure with code = ApiErrorCodes.authAlreadyRegistered), AuthInvalidToken (extends AuthFailure), AuthCancelled (extends AppFailure for the user-cancel path).",
    "frontend/lib/features/auth/application/sign_in_controller.dart is @riverpod class SignInController extends _$SignInController with @override AsyncValue<void> build() returning const AsyncData(null), and a method Future<void> signIn() that wraps the AAD login → repository.completeSignIn → authSession.markSignedIn flow in AsyncValue.guard. AuthCancelled is mapped to AsyncData(null) (silent). All other AppFailure subclasses propagate as AsyncError.",
    "frontend/lib/features/auth/presentation/sign_in_page.dart is a const-constructible ConsumerWidget. It renders: app logo (placeholder), tagline 'Travel together. Meet briefly.', primary button 'Sign in with Wander' (disabled while signInControllerProvider.isLoading == true), and an error banner when signInControllerProvider.hasError. All colors via AppColors.brand, all spacing via AppSpacing tokens. No raw color/size literals.",
    "frontend/lib/features/auth/auth_routes.dart exports a const list authRoutes containing GoRoute for AppRoutes.signIn (name AppRouteNames.signIn) → SignInPage. Imported and merged into router.dart.",
    "test/features/auth/presentation/sign_in_page_test.dart: widget test that pumps SignInPage inside ProviderScope with overrides for aadOAuthClientProvider (returns a fixed AadLoginResult) and authRepositoryProvider (returns a fixed UserDto). Asserts: button visible; tap shows progress; on success the GoRouter (driven by a fake ref.listen on authSessionProvider) is invoked with goNamed(AppRouteNames.onboardingWelcome). Verify NO real HTTP and NO real secure storage are touched (provider overrides only).",
    "test/features/auth/presentation/sign_in_page_golden_test.dart: golden at iPhone 15 (393x852) and Pixel 7 (412x915) for the idle, loading, and error states.",
    "test/core/network/auth_interceptor_test.dart: unit test against a Dio configured with the interceptor + DioAdapter (http_mock_adapter). Asserts (a) Bearer header injected for /api/v1/discover; (b) NO Bearer header for /api/v1/auth/register; (c) NO Bearer header for /api/v1/auth/refresh; (d) when token is missing on a non-auth path, the request fails with AppFailureException(AuthFailure).",
    "test/core/network/refresh_interceptor_test.dart: unit tests for (a) 401 → refresh succeeds → original retried with NEW token (not the stale one in requestOptions.headers); (b) two concurrent 401s only POST /auth/refresh ONCE (assert via mock-adapter call count); (c) refresh returns 401 → tokenStorage.clear called, authSession.signOut called with reason=sessionExpired, AppFailureException(AuthFailure) thrown; (d) /api/v1/auth/refresh path itself does NOT enter the refresh loop on 401 (avoid recursion).",
    "test/core/network/api_error_interceptor_test.dart: unit test mapping table — for each input { status, body } assert the resulting AppFailure subclass + code. Cover: 400 with FastEndpoints body {'Validation.FirstNameRequired': ['First name is required']} → ValidationFailure(code: 'Validation.FirstNameRequired'); 401 → AuthFailure; 403 → ForbiddenFailure; 404 → NotFoundFailure; 409 with {'Auth.AlreadyRegistered': [...]} → ConflictFailure(code: 'Auth.AlreadyRegistered'); 429 with Retry-After: 30 → RateLimitFailure(retryAfter: Duration(seconds: 30)); 500 → ServerFailure; connection refused → NetworkFailure.",
    "test/features/auth/application/sign_in_controller_test.dart: ProviderContainer-based unit tests. (a) happy path: signIn() transitions AsyncData(null) → AsyncLoading → AsyncData(null), authSessionProvider becomes AuthSignedIn; (b) AAD cancel: signIn() returns to AsyncData(null), authSession remains signedOut; (c) register 401: AsyncError(AuthInvalidToken); (d) register 409 (returning user): AsyncData(null) + authSession AuthSignedIn (the 409 is treated as success).",
    "test/features/auth/data/auth_api_repository_test.dart: unit test using a fake AuthApi. Asserts tokenStorage.write is called BEFORE authApi.register (order matters — AuthInterceptor reads from storage). Asserts 409 returns a UserDto without throwing.",
    "test/core/auth/token_storage_test.dart: unit test using a fake FlutterSecureStorage. Asserts read/write/clear; asserts logger output (intercepted via the shared logger's StreamRecord) NEVER contains the token strings.",
    "frontend/integration_test/uc_401_signin_test.dart: integration test that boots the app with a shelf HTTP server canned for POST /api/v1/auth/register → 201 + UserDto. The test overrides aadOAuthClientProvider to return a fixed login result without launching a browser. Asserts: app starts on /sign-in (no token in storage); tap 'Sign in with Wander'; loader appears; app navigates to /onboarding/welcome (the placeholder route in this UC); tokens are present in tokenStorage. The test asserts that interceptors ran for real (the register POST on the shelf server received Bearer header equal to the fixed access token).",
    "frontend/pubspec.yaml gains exactly these new runtime dependencies: dio (latest 5.x), aad_oauth (latest 1.x), flutter_secure_storage (latest 9.x), go_router (latest 16.x). New dev_dependencies: http_mock_adapter, shelf, golden_toolkit. freezed + json_serializable + freezed_annotation + json_annotation are added as runtime / dev as appropriate. Versions are pinned to the resolver's selection at scaffold time and committed in pubspec.lock; no caret bumps.",
    "Codegen is up to date: dart run build_runner build --delete-conflicting-outputs produces no diff after the slice is implemented; .g.dart and .freezed.dart files are committed.",
    "flutter analyze is clean against frontend/. flutter test (full suite, including the new tests in this UC) is green. flutter test integration_test/uc_401_signin_test.dart is green."
  ],
  "out_of_scope": [
    "Onboarding screens / flow (UC-402). Post-sign-in this UC redirects to a placeholder /onboarding/welcome route returning a 'Onboarding (UC-402)' Scaffold. UC-402 will replace it.",
    "Stripe Identity ID verification (UC-403).",
    "Profile photo upload (UC-404).",
    "Public profile and own profile screens (UC-405, UC-406). The /users/me lookup is stubbed in this UC — completeSignIn returns a synthetic UserDto containing only the JWT sub-derived id and firstName from the AAD claims. UC-405 wires the real /users/me endpoint.",
    "City selection / travel history (UC-407).",
    "Discovery, invites, places, meetups, reviews, reports — all later UCs.",
    "SignalR client wiring (UC-504). Sign-out does NOT yet disconnect a hub since none is connected.",
    "FCM token registration on app open (UC-505). Sign-out does NOT yet PATCH a null token.",
    "RevenueCat subscription state (UC-604). The UserDto isPremium field, if surfaced by the backend, is read but unused.",
    "Crash reporting integration (Phase 6 polish). Logger writes to stdout in dev; production wiring is later.",
    "Localization. Error messages are English-only via error_messages.dart constants. flutter_localizations is not added.",
    "Cert-pinning the Dio client. Phase 6 hardening.",
    "Backend changes. UC-401 consumes the existing UC-201 endpoints unchanged. If a missing error code is needed (e.g. Auth.InvalidToken does not yet exist on the backend), add it to src/WanderMeet.Shared/ErrorCodes.cs in the same UC — but do NOT change endpoint behaviour."
  ],
  "non_functional": [
    "Tokens (access + refresh) MUST NOT appear in any log line, print() call, or analytics/crash payload. Logger setup in core/logging/logger.dart applies a redaction filter on any string containing 'eyJ' (JWT prefix) — replace with '<REDACTED-JWT>'.",
    "Single in-flight refresh: under concurrent 401s the count of POST /api/v1/auth/refresh equals one. Verified in test/core/network/refresh_interceptor_test.dart case (b).",
    "AuthInterceptor reads the token from tokenStorage on every request — never caches it in memory across the interceptor lifetime — so a refresh-completed-mid-flight does not lose the new token.",
    "Sign-in cold-start to /sign-in render: target < 1.5 s on a Pixel 4a / iPhone 12. Bootstrap is two awaits (Hive.initFlutter, env-provider readiness) and one synchronous router build.",
    "Sign-in B2C round-trip is bounded by the user's network and B2C tenant; no client-side timeout shorter than 30 s.",
    "Storage durability: tokens use Keychain on iOS (default secure_storage) and EncryptedSharedPreferences on Android (set IOSOptions(accessibility: KeychainAccessibility.first_unlock) and AndroidOptions(encryptedSharedPreferences: true) at the secure_storage construction site).",
    "PII handling: firstName from B2C claims is sent to backend POST /auth/register and never logged client-side beyond authSession.firstName which is referenced by UI rendering only.",
    "Test isolation: every widget/integration/unit test in this UC uses ProviderScope/ProviderContainer overrides — no test touches real flutter_secure_storage (use a FakeSecureStorage in core/auth/token_storage_test.dart) and no test touches a real B2C tenant.",
    "Codegen freshness is part of the gate: CI runs dart run build_runner build --delete-conflicting-outputs && git diff --exit-code; this UC must be committable through that gate."
  ]
}
